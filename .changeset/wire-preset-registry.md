---
"@flemma-dev/flemma.nvim": minor
---

Added tool approval presets configuration field and wired preset registry into plugin initialization with `{ "$default" }` as the default auto_approve policy
