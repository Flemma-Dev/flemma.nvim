---
"@flemma-dev/flemma.nvim": minor
---

Added `<Space>` keymap to toggle folds in `.chat` buffers. Configurable via `keymaps.normal.fold_toggle`; automatically skipped when the key conflicts with `mapleader`.
