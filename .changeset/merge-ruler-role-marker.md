---
"@flemma-dev/flemma.nvim": minor
---

Merged ruler into role marker lines: `@Role:` now renders as `─ Role ─────...` with the ruler extending to the window edge, replacing the separate virtual line above each message
