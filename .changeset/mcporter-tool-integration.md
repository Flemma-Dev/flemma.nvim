---
"@flemma-dev/flemma.nvim": minor
---

Added mcporter tool integration: dynamically discovers MCP servers and registers their tools as Flemma tool definitions. Configure via `tools.mcporter` with include/exclude glob patterns. Disabled by default.
