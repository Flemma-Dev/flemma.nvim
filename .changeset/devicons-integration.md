---
"@flemma-dev/flemma.nvim": minor
---

Added devicons integration that auto-registers a .chat file icon with nvim-web-devicons (or other compatible devicons plugins). Enabled by default — configure via `integrations.devicons.enabled` and `integrations.devicons.icon`.
