---
"@flemma-dev/flemma.nvim": minor
---

Added `secrets.gcloud.path` config option to override the gcloud binary path, and introduced the generic `flemma.config.ConfigAware<T>` interface with `flemma.secrets.Context` for typed per-resolver config access
