---
"@flemma-dev/flemma.nvim": minor
---

Changed the default `turns.padding` from `{ left = 1, right = 0 }` to `{ left = 0, right = 1 }` so the turn indicator hugs the sign column with breathing room on the right.
