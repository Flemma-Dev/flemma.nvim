---
"@flemma-dev/flemma.nvim": minor
---

Added support for `model = "$preset-name"` in config to use a preset as the startup default, avoiding duplication of provider/model/parameters at the top level
