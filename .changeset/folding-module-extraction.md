---
"@flemma-dev/flemma.nvim": minor
---

Extracted folding logic into dedicated `ui/folding` module with registry-based fold rules, O(1) cached fold map, and configurable `auto_close` per fold type (thinking, tool_use, tool_result, frontmatter)
