---
"@flemma-dev/flemma.nvim": patch
---

Fix `provider/model` shorthand (e.g., `flemma.opt.model = "anthropic/claude-opus-4-8"`) in setup opts and frontmatter — previously only worked in `:Flemma switch` commands.
