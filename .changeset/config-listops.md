---
"@flemma-dev/flemma.nvim": minor
---

Added op-prefix syntax for list-valued config fields (+append, ^prepend, !remove, $spread from preset). Presets now support a `tools` field for controlling available tools via `:Flemma switch`.
