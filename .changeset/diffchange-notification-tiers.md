---
"@flemma-dev/flemma.nvim": minor
---

Notification bar now derives all colors from DiffChange with three foreground tiers (primary, secondary, muted) and WCAG contrast enforcement on semantic cache colors. Added `^` contrast operator to highlight expressions and extracted color utilities into `flemma.utilities.color` for reuse.
