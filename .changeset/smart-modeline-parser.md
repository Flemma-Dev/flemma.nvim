---
"@flemma-dev/flemma.nvim": minor
---

Extended the modeline parser with quote-aware tokenization, type coercion for positional arguments, single and double quote support with backslash escaping, comma-separated list values, and empty value handling (`key=` → nil, `key=""` → empty string).
