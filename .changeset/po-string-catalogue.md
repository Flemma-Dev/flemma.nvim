---
"@flemma-dev/flemma.nvim": patch
---

Externalized conversation messages and tool definition strings into a gettext PO catalogue (`po/flemma.po`). Rendered strings are unchanged; the catalogue is the groundwork for future user overrides and translations.
