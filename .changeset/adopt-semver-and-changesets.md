---
"@flemma-dev/flemma.nvim": minor
---

Adopted semantic versioning (semver) and changesets for automated version management and changelog generation. The project transitions from the previous CalVer (`vYY.MM-N`) scheme to standard semver, starting at `0.1.0`.
