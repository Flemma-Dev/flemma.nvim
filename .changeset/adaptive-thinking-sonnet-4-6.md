---
"@flemma-dev/flemma.nvim": patch
---

Fixed Sonnet 4.6 to use adaptive thinking instead of deprecated budget_tokens, clamped `max` effort to `high` on non-Opus models, and added budget_tokens < max_tokens guard for budget-based models
