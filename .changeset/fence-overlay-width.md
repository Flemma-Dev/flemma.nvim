---
"@flemma-dev/flemma.nvim": patch
---

Fixed fence overlay rendering for wide and nested code fences — bar now scales to the actual backtick count, and inner backtick lines inside a wider fence are no longer overlaid
